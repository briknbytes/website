# Copy to .dev.vars for local `npm run worker:dev` testing (never commit .dev.vars).
# In production, set these as Cloudflare Worker runtime variables/secrets instead.

DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_GUILD_ID=
DISCORD_REDIRECT_URI=http://localhost:8787/api/auth/callback
SESSION_SECRET=change-me-to-a-long-random-string
SITE_URL=http://localhost:8787
